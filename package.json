{
  "name": "bingo",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.18.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "clear && bunx chokidar-cli '{gameHost,player,gameAdmin}/**/*.*' -c 'bun build.ts' --initial & bun --watch server/index.ts",
    "prod": "bun run server/index.ts"
  },
  "dependencies": {
    "@types/qrcode": "^1.5.6",
    "ws": "^8.21.0"
  }
}